create table item
(
   batch varchar(255),
   industry varchar(255),
   category varchar(255),
   health varchar(255),
   code varchar(255),
   name varchar(255),
   scope varchar(255),
   logout varchar(255),
   status varchar(255)
);